import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

createApp(App).use(router).mount('#app')
// Semana 4 - Commit 1: Configuração de bootstrap da aplicação via main.js

